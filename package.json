{
  "name": "mf-test",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "dev:host": "turbo run dev --filter=host",
    "dev:web1": "turbo run dev --filter=web1",
    "dev:web2": "turbo run dev --filter=web2",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.6.1",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
